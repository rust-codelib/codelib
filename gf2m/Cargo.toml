[package]
name = "gf2m"
version = "1.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Конечные поля GF(2^m), m = 2..=16: const-generic ядро, log/exp-таблицы, clmul, рантайм-слой (Ито–Цудзии), no_std"
keywords = ["galois", "finite-field", "no_std", "math"]
categories = ["mathematics", "no-std", "algorithms", "embedded"]

[dependencies]
# пусто — только core

[dev-dependencies]
proptest.workspace = true
criterion.workspace = true

[[bench]]
name = "field"
harness = false

[lib]
name = "gf2m"
path = "src/lib.rs"
